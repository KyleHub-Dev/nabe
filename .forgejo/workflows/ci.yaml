name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  checks:
    runs-on: docker
    container:
      image: node:lts-bookworm
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.0
      - run: pnpm install --frozen-lockfile=false
      - run: apt-get update && apt-get install -y cargo golang-go
      - run: pnpm lint
      - run: pnpm build
      - run: pnpm test
