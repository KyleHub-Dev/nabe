# Speiche Agent

Speiche is the Apache-2.0 edge connector for Nabe.

This is an early stub. It loads configuration from environment variables, prints startup information, prepares a placeholder heartbeat client, and includes a placeholder local AdGuard driver. It exposes no public server by default.
