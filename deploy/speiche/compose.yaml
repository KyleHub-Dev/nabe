services:
  speiche:
    image: speiche:dev
    build:
      context: ../..
      dockerfile: apps/speiche/Dockerfile
    env_file: .env

# Optional local DNS Engine for development can be added here.
# Keep native engine UI private unless intentionally debugging.
